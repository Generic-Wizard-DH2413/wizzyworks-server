services:
  wizzyworks-server:
    build: .
    ports:
      - "8765:8765"  # WebSocket port
      - "8000:8000"  # Health check port
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
    networks:
      - wizzyworks-network

networks:
  wizzyworks-network:
    driver: bridge